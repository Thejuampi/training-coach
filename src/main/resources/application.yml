spring:
  application:
    name: training-coach
  profiles:
    active: dev

server:
  port: 8080

logging:
  level:
    root: INFO
    com.training.coach: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

intervals:
  icu:
    base-url: https://intervals.icu
    api-key: ${INTERVALS_ICU_API_KEY}
